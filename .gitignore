node_modules

# Local environment overrides (copy from .env.example)
.env
server/.env

# Build output
client/dist
server/dist

# SQLite database (runtime data, not source)
server/*.db
server/*.db-shm
server/*.db-wal

# Claude Code project settings (personal tooling config)
.claude/settings.json
sermontracker.tar.gz
